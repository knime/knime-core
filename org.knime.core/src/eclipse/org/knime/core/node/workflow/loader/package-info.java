/**
 * Provides functionality to load workflows into a POJO-based descriptive format (*Def classes, see for instance
 * RootWorkflowDef).
 *
 * This implementation reads the original KNIME workflow file format based on nested directories on a file system and
 * the corresponding xml files to describe workflows and nodes.
 */
package org.knime.core.node.workflow.loader;
