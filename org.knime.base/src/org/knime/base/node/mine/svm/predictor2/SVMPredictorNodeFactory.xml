<?xml version="1.0" encoding="utf-8"?>
<knimeNode icon="./SVM_predict.png" type = "Predictor" xmlns="http://knime.org/node/v2.8"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://knime.org/node/v2.8 http://knime.org/node/v2.8.xsd">
	<name>SVM Predictor</name>
	<shortDescription>
	    This node uses a SVM model generated by the SVM learner
		node to predict the output for given parameters.
	</shortDescription>
	
	<fullDescription>
	<intro>
		This node uses a SVM model generated by the SVM learner
		node to predict the output for given values. Only the columns
		that have been used during training are used in
		the SVM learner. The output table will contain one extra
		column, which contains the prediction and optinally the class
		probabilities.
		</intro>
		<option name="Change prediction column name">
			When set, you can change the name of the prediction column.
		</option>
		<option name="Prediction column">
			The possibly overridden column name for the predicted column. (The
			default is:
			<tt>Prediction (</tt>trainingColumn<tt>)</tt>.)
		</option>
		<option name="Append Class Columns">
			Append class probability columns.
		</option>
		<option name="Suffix for probability columns">
			Suffix for the normalized distribution columns. Their names are like:
			<tt>P (</tt>trainingColumn<tt>=</tt>value<tt>)</tt>.
		</option>
	</fullDescription>

	<ports>
		<inPort index="0" name="Trained SVM">The model generated by the SVM Learner
		</inPort>
		<inPort index="1" name="Test Data">Inport for the input table</inPort>
		<outPort index="0" name="Classification">The input table, with (the class
			probabilities and) the prediction column appended to it.
		</outPort>
	</ports>
</knimeNode>