<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE knimeNode PUBLIC "-//UNIKN//DTD KNIME Node 1.0//EN" "http://www.knime.org/Node.dtd" >
<knimeNode icon="groupBy.png" type="Manipulator">
    <name>GroupBy</name>
    <shortDescription>
    	Groups the table by the selected column(s) and aggregates the remaining
    	columns using the selected aggregation method.
    </shortDescription>
    <fullDescription>
    	<intro>
    	   <p>
    	   Groups the rows of a table by the unique values in the selected columns.
    	    A row is created for each unique value group of the selected column(s). 
    	    The remaining rows are aggregated by the defined method.
            The output table therefore contains one row for each existing value 
            combination of the selected columns. The values in the other 
            (non-selected) columns in this row depend on the selected 
            aggregation method.
           </p>
           <p>
            Available aggregation methods:
    		<ul>
    			<li>First: Takes the first entry per group.</li>
    			<li>First value: Takes the first value that is not a missing value per group.</li>
    			<li>Last: Takes the last entry per group.</li>
    			<li>Last value: Takes the last value that is not a missing value per group.</li>
    			<li>Mode: Takes the value with the most occurrences per
    			 group. If multiple values match this criterion 
    			 the first occurrence is written to the output table.</li>
    			 <li>Concatenate: Concatenates the values per group.</li>
    			 <li>Unique concatenate: Concatenates each value only once 
    			 per group.</li>
    			 <li>Unique count: Counts each value only once per group.</li>
    			<li>Count: Counts members per group.</li>
    		</ul>
			Additional aggregation methods only available for numerical columns:
			<ul>
    			<li>Minimum: Calculates the minimum value per group.</li>
    			<li>Maximum: Calculates the maximum value per group.</li>
    			<li>Mean: Calculates the mean value per group.</li>
    			<li>Sum: Calculates the sum per group.</li>
    			<li>Variance: Calculates the variance per group.</li>
    			<li>Standard deviation: Calculates the standard deviation 
    			per group.</li>
    		</ul>
    		</p>
    		<p>
            If the aggregation method 'Mode' or 'Unique concatenate' 
            is selected, a maximum number 
            of different values can be defined to be stored in memory. 
            If the actual number of values exceeds this limit, 
            the calculation for this group is skipped, a missing value is set 
            in the corresponding column, and a warning is displayed. 
            </p>
            <p>
            If the 'Sort in memory' option is checked the complete table is 
            loaded into the memory to speed up the sorting process.
            </p>
    	</intro>	
        <option name="Nominal aggregation method">
        	The aggregation method to use for all non-selected columns
        	that do not contain numerical values.
        </option>
        <option name="Numeric aggregation method">
        	The aggregation method to use for all non-selected numerical 
        	columns.
        </option>
        <option name="Maximum unique values per group">
	        Defines the maximum number of unique values per group to avoid 
	        problems with memory overloading. This option is only enabled if a 
	        memory-consuming method like Mode is selected. All groups with more unique
	        values are skipped during the calculation and a missing value is set 
            in the corresponding column.
        </option>
        <option name="Move group column(s) to front">
			Moves the selected group column(s) to the front of the result table.
        </option>
        <option name="Sort in memory">
			Performs necessary sorting in the memory. Speeds up the process
			but consumes much more memory.
        </option>
        <option name="Enable hiliting">
			If enabled, the hiliting of a group row will hilite all rows of this
			group in other views. Depending of the number of rows, enabling this 
			feature might consume a lot of memory.
        </option>
        <option name="Group by column(s)">
	        Select one or more column(s) according to which the group(s) 
	        is/are created.
		</option>
    </fullDescription>
    <ports>
        <dataIn index="0" name="Data table">
	        The input table to group.
        </dataIn>
        <dataOut index="0" name="Group table">
        	Result table with one row for each existing value combination of 
        	the selected columns.
		</dataOut>
    </ports>
</knimeNode>
